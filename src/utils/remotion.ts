// Convenience re-exports and thin wrappers for Remotion primitives.
// Import from here rather than remotion directly for consistent usage.
export {
  AbsoluteFill,
  Composition,
  Img,
  Sequence,
  interpolate,
  spring,
  staticFile,
  useCurrentFrame,
  useVideoConfig,
} from "remotion";
